# DGX-Request-Hub
A web app to manage DGX compute access: users submit GPU/MIG requests, admins manually update available capacity and MIG partitions, and the system allocates requests or assigns a waitlist number with status tracking and notifications
